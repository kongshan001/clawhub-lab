# gh-issues Skill 深度分析

> 📅 分析日期: 2026-02-28
> ⭐ 评分: N/A (OpenClaw 内置)
> 🔗 来源: OpenClaw 内置 Skill

---

## 基本信息

| 属性 | 值 |
|------|-----|
| 名称 | gh-issues |
| 功能 | 自动修复 GitHub Issues，生成 PR，处理 PR 审查 |
| 依赖 | curl, git, gh |
| 主要环境变量 | GH_TOKEN |

## 核心功能

### 6 阶段工作流

```
Phase 1: 解析参数
Phase 2: 获取 Issues
Phase 3: 展示确认
Phase 4: 预检查
Phase 5: 生成子代理
Phase 6: PR 审查处理
```

## 使用方式

```bash
/gh-issues [owner/repo] [options]
```

### 参数说明

| 参数 | 默认值 | 说明 |
|------|--------|------|
| `--label` | 无 | 按标签过滤 |
| `--limit` | 10 | 每次获取的最大 Issue 数 |
| `--milestone` | 无 | 按里程碑过滤 |
| `--assignee` | 无 | 按指派人过滤 (`@me` 为自己) |
| `--fork` | 无 | 指定 fork 仓库推送分支 |
| `--watch` | false | 持续轮询新 Issues |
| `--interval` | 5 | 轮询间隔（分钟） |
| `--dry-run` | false | 仅展示，不执行 |
| `--yes` | false | 跳过确认 |
| `--reviews-only` | false | 仅处理 PR 审查 |
| `--cron` | false | Cron 安全模式 |
| `--model` | 无 | 子代理使用的模型 |
| `--notify-channel` | 无 | Telegram 通知频道 |

## 核心特性

### 1. 并行子代理

- 最多同时启动 8 个子代理
- 每个子代理 60 分钟超时
- 自动创建分支、提交、推送、创建 PR

### 2. Fork 模式

```bash
/gh-issues owner/repo --fork your-fork/repo
```

- Issues 从源仓库获取
- 分支推送到 fork
- PR 从 fork 到源仓库

### 3. Watch 模式

```bash
/gh-issues owner/repo --watch --interval 10
```

- 持续监控新 Issues
- 定期检查 PR 审查评论
- 自动处理变更请求

### 4. Cron 模式

```bash
/gh-issues owner/repo --cron
```

- 适合定时任务
- 每次处理一个 Issue
- 不等待结果，fire-and-forget

## 子代理任务流程

```
1. 置信度检查 (confidence < 7 则跳过)
2. 理解 Issue
3. 创建分支 fix/issue-{N}
4. 分析代码库
5. 实现修复
6. 运行测试
7. 提交变更
8. 推送分支
9. 创建 PR
10. 发送报告
```

## PR 审查处理

### 评论来源

- PR reviews
- 内联审查评论
- Issue 评论
- PR body 中的嵌入式审查（如 Greptile）

### 可操作评论判断

**不可操作：**
- 纯批准或 "LGTM"
- 机器人信息评论
- 已处理的评论

**可操作：**
- `CHANGES_REQUESTED` 状态
- 包含 "please fix", "change this", "needs to" 等
- 指出具体代码问题

## 预检查步骤

1. 工作区脏检查
2. 记录基础分支
3. 验证远程访问
4. 验证 GH_TOKEN 有效性
5. 检查现有 PR
6. 检查进行中的分支
7. 检查 claims 文件（防止重复处理）

## 输出示例

```
| Issue                 | Status    | PR                             |
| --------------------- | --------- | ------------------------------ |
| #42 Fix null pointer  | PR opened | https://github.com/.../pull/99 |
| #37 Add retry logic   | Failed    | --                             |
| #15 Update docs       | Timed out | --                             |

Processed 3 issues: 1 PRs opened, 1 failed, 1 skipped.
```

## 约束

- 不使用 gh CLI（仅用 curl + GitHub REST API）
- 不强制推送
- 不添加无关变更
- 不添加新依赖（除非必要）
- 60 分钟超时

---

*由 ClawHub Lab Agent 分析*
