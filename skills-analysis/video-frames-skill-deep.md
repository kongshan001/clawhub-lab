# Video Frames Skill 深度分析

**分析日期**: 2026-03-01
**Skill 类型**: OpenClaw 内置 Skill
**评分**: N/A (内置)
**依赖**: ffmpeg

---

## 1. 概述

**video-frames** 是一个轻量级的视频帧提取工具，基于 ffmpeg 实现。它允许用户从视频文件中快速提取单帧图像，支持按时间戳或帧索引定位。

### 核心价值

- 🎞️ **快速帧提取** - 从任意视频提取单帧
- ⏱️ **时间戳定位** - 精确到秒的帧定位
- 📊 **帧索引定位** - 按帧序号精确提取
- 🖼️ **灵活输出** - 支持 JPG/PNG 格式

---

## 2. 技术架构

### 2.1 目录结构

```
video-frames/
├── SKILL.md          # Skill 定义文档
└── scripts/
    └── frame.sh      # 帧提取脚本
```

### 2.2 核心脚本分析

**frame.sh** 是一个纯 Bash 脚本，封装了 ffmpeg 的帧提取功能：

```bash
# 核心命令模式
ffmpeg -hide_banner -loglevel error -y \
  -i "$in" \           # 输入视频
  -vf "select=eq(n\\,${index})" \  # 帧选择过滤器
  -vframes 1 \         # 只提取1帧
  "$out"               # 输出文件
```

### 2.3 三种提取模式

| 模式 | 参数 | ffmpeg 命令 | 用途 |
|------|------|-------------|------|
| 首帧 | 无参数 | `-vf "select=eq(n\\,0)"` | 快速预览 |
| 时间戳 | `--time HH:MM:SS` | `-ss "$time" -frames:v 1` | 定点检查 |
| 帧索引 | `--index N` | `-vf "select=eq(n\\,$index)"` | 精确帧提取 |

---

## 3. 使用场景

### 3.1 视频内容审核

```bash
# 检查视频第10秒的内容
frame.sh suspicious.mp4 --time 00:00:10 --out /tmp/check.jpg
```

### 3.2 缩略图生成

```bash
# 生成视频封面（首帧）
frame.sh presentation.mp4 --out /tmp/thumbnail.jpg
```

### 3.3 视频帧分析

```bash
# 提取特定帧进行 AI 分析
frame.sh demo.mp4 --index 150 --out /tmp/frame150.png
```

### 3.4 批量处理

```bash
# 提取多个时间点的帧
for t in 00:00:05 00:00:10 00:00:15 00:00:20; do
  frame.sh video.mp4 --time $t --out "/tmp/frame-${t}.jpg"
done
```

---

## 4. 与 OpenClaw Agent 的集成

### 4.1 自动化工作流

```yaml
# 示例：视频监控自动审核
workflow:
  trigger: new_video_uploaded
  steps:
    - action: video-frames/extract
      params:
        time: "00:00:05"
        out: "/tmp/preview.jpg"
    - action: image/analyze
      params:
        image: "/tmp/preview.jpg"
        prompt: "检测是否有不当内容"
```

### 4.2 与其他 Skill 的协作

| Skill | 协作场景 |
|-------|----------|
| **openai-image-gen** | 帧提取后进行图像增强 |
| **gemini** | 帧提取后进行视觉分析 |
| **summarize** | 提取关键帧后生成视频摘要 |
| **notion** | 将帧保存到笔记中 |

---

## 5. 性能特性

### 5.1 优化设计

- **`-hide_banner -loglevel error`**: 减少 I/O 开销
- **`-y`**: 自动覆盖，避免交互
- **单帧提取**: 只处理需要的帧，不解码整个视频

### 5.2 格式建议

| 格式 | 优点 | 缺点 | 推荐场景 |
|------|------|------|----------|
| **JPG** | 文件小，分享快 | 有损压缩 | 快速预览、分享 |
| **PNG** | 无损，细节清晰 | 文件较大 | UI 截图、文字内容 |

---

## 6. 依赖与安装

### 6.1 系统要求

```bash
# Ubuntu/Debian
apt install ffmpeg

# macOS
brew install ffmpeg

# CentOS/RHEL
yum install ffmpeg
```

### 6.2 Skill 元数据

```yaml
metadata:
  openclaw:
    emoji: 🎞️
    requires:
      bins: [ffmpeg]
    install:
      - id: brew
        kind: brew
        formula: ffmpeg
        bins: [ffmpeg]
```

---

## 7. 最佳实践

### 7.1 时间戳选择

```bash
# ✅ 推荐：使用 -ss 在 -i 之前（更快，seek 到位置后才开始解码）
ffmpeg -ss 00:01:30 -i video.mp4 -frames:v 1 out.jpg

# ⚠️ 当前实现：-ss 在 -i 之后（更精确，但需要解码到该位置）
ffmpeg -i video.mp4 -ss 00:01:30 -frames:v 1 out.jpg
```

### 7.2 批量提取优化

```bash
# 使用 fps 过滤器均匀采样
ffmpeg -i video.mp4 -vf "fps=1/10" -frames:v 5 thumb_%d.jpg
# 每10秒提取一帧，共5帧
```

### 7.3 错误处理

```bash
# 脚本已包含的错误检查
if [[ ! -f "$in" ]]; then
  echo "File not found: $in" >&2
  exit 1
fi
```

---

## 8. 潜在改进方向

### 8.1 功能扩展

| 功能 | 描述 | 优先级 |
|------|------|--------|
| 多帧批量提取 | 一次提取多个时间点 | 高 |
| 智能采样 | 自动选择关键帧 | 中 |
| GIF 生成 | 提取帧序列生成 GIF | 中 |
| 视频信息 | 获取视频元数据 | 低 |

### 8.2 性能优化

```bash
# 建议改进：-ss 放在 -i 之前
# 当前
ffmpeg -ss "$time" -i "$in" -frames:v 1 "$out"

# 更快的方案（对于长视频）
ffmpeg -ss "$time" -i "$in" -frames:v 1 -q:v 2 "$out"
```

---

## 9. 实际应用示例

### 9.1 视频监控场景

```bash
# 提取视频开头、中间、结尾三帧
duration=$(ffprobe -v error -show_entries format=duration -of default=noprint_wrappers=1:nokey=1 video.mp4)
mid=$(echo "$duration / 2" | bc)

frame.sh video.mp4 --out /tmp/start.jpg
frame.sh video.mp4 --time 00:00:$mid --out /tmp/middle.jpg
frame.sh video.mp4 --index $(echo "$duration * 30 - 1" | bc | cut -d. -f1) --out /tmp/end.jpg
```

### 9.2 与 AI 分析集成

```bash
# 提取帧后调用 OpenClaw 的 image 工具
frame.sh demo.mp4 --time 00:00:30 --out /tmp/analyze.png

# 然后 Agent 可以使用 image 工具分析
# image(prompt="描述这个视频帧的内容", image="/tmp/analyze.png")
```

---

## 10. 总结

### 优势

| 方面 | 评价 |
|------|------|
| **简洁性** | ⭐⭐⭐⭐⭐ 单一职责，易于理解 |
| **实用性** | ⭐⭐⭐⭐⭐ 视频处理的基础需求 |
| **可靠性** | ⭐⭐⭐⭐⭐ 基于 ffmpeg，稳定可靠 |
| **集成性** | ⭐⭐⭐⭐ 可与 OpenClaw 其他 Skill 良好配合 |

### 适用场景

- ✅ 视频内容快速预览
- ✅ 视频审核和监控
- ✅ 缩略图生成
- ✅ 视频帧分析
- ✅ 自动化视频处理流程

### 不适用场景

- ❌ 复杂视频编辑
- ❌ 视频转码
- ❌ 音频提取
- ❌ 批量帧序列提取（需要多次调用）

---

## 11. 安装状态

| 项目 | 状态 |
|------|------|
| Skill 文件 | ✅ 已安装 |
| ffmpeg 依赖 | ❌ 未安装 |

**安装命令**:
```bash
apt install ffmpeg  # Ubuntu/Debian
```

---

*分析完成于 2026-03-01 by Glint 🔺*
