# Discord Skill 深度分析报告

**分析日期**: 2026-03-02  
**Skill 名称**: discord  
**评分**: 内置 Skill  
**分析者**: Glint 🔺

---

## 1. 概述

Discord Skill 是 OpenClaw 的内置通信集成，通过 `message` 工具提供完整的 Discord 机器人功能。无需额外安装，只需配置 Discord Bot Token 即可使用。

### 核心特性

- 🎮 **完整消息操作**: 发送、编辑、删除、回复
- 💬 **富媒体支持**: 图片、视频、文件附件
- 🧩 **组件 v2**: 支持 Discord 最新的交互式组件
- 📊 **投票系统**: 创建多选项投票
- 🧵 **线程管理**: 创建和管理讨论线程
- 🔍 **消息搜索**: 跨频道搜索历史消息
- 🎭 **状态管理**: 设置机器人在线状态和活动

---

## 2. 配置要求

### 必需配置

```yaml
# config.yaml
channels:
  discord:
    token: "YOUR_DISCORD_BOT_TOKEN"
```

### 可选权限门控

```yaml
channels:
  discord:
    actions:
      roles: false        # 角色管理（默认关闭）
      moderation: false   # 审核操作（默认关闭）
      presence: false     # 状态设置（默认关闭）
      channels: false     # 频道管理（默认关闭）
```

---

## 3. 核心 API

### 3.1 发送消息

```json
{
  "action": "send",
  "channel": "discord",
  "to": "channel:123456789",
  "message": "Hello, Discord!"
}
```

**参数说明**:
- `to`: 目标标识，格式为 `channel:<id>` 或 `user:<id>`
- `message`: 消息文本内容
- `silent`: 可选，设为 `true` 静默发送（不触发通知）

### 3.2 发送富媒体

```json
{
  "action": "send",
  "channel": "discord",
  "to": "channel:123456789",
  "message": "查看附件",
  "media": "file:///tmp/screenshot.png"
}
```

**支持的媒体格式**:
- 图片: PNG, JPG, GIF, WebP
- 视频: MP4, WebM
- 文件: 任意格式（受 Discord 文件大小限制）

### 3.3 Discord 组件 v2（推荐）

```json
{
  "action": "send",
  "channel": "discord",
  "to": "channel:123456789",
  "message": "状态更新",
  "components": "[Carbon v2 组件实例]"
}
```

**注意**:
- `components` 期望 Carbon 组件实例（Container, TextDisplay 等）
- 不要将 `components` 与 `embeds` 混用（Discord 会拒绝）

### 3.4 消息反应

```json
{
  "action": "react",
  "channel": "discord",
  "channelId": "123456789",
  "messageId": "987654321",
  "emoji": "✅"
}
```

### 3.5 读取消息

```json
{
  "action": "read",
  "channel": "discord",
  "to": "channel:123456789",
  "limit": 20
}
```

### 3.6 编辑消息

```json
{
  "action": "edit",
  "channel": "discord",
  "channelId": "123456789",
  "messageId": "987654321",
  "message": "修正后的内容"
}
```

### 3.7 删除消息

```json
{
  "action": "delete",
  "channel": "discord",
  "channelId": "123456789",
  "messageId": "987654321"
}
```

---

## 4. 高级功能

### 4.1 创建投票

```json
{
  "action": "poll",
  "channel": "discord",
  "to": "channel:123456789",
  "pollQuestion": "午餐吃什么？",
  "pollOption": ["披萨", "寿司", "沙拉", "其他"],
  "pollMulti": false,
  "pollDurationHours": 24
}
```

**参数说明**:
- `pollMulti`: 是否允许多选
- `pollDurationHours`: 投票持续时间（小时）

### 4.2 固定消息

```json
{
  "action": "pin",
  "channel": "discord",
  "channelId": "123456789",
  "messageId": "987654321"
}
```

### 4.3 创建线程

```json
{
  "action": "thread-create",
  "channel": "discord",
  "channelId": "123456789",
  "messageId": "987654321",
  "threadName": "Bug 分类讨论"
}
```

### 4.4 搜索消息

```json
{
  "action": "search",
  "channel": "discord",
  "guildId": "999888777",
  "query": "发布说明",
  "channelIds": ["123456789", "987654321"],
  "limit": 10
}
```

### 4.5 设置状态（需启用权限）

```json
{
  "action": "set-presence",
  "channel": "discord",
  "activityType": "playing",
  "activityName": "with fire",
  "status": "online"
}
```

**活动类型**:
- `playing`: 正在玩
- `streaming`: 正在直播（需 `activityUrl`）
- `listening`: 正在听
- `watching`: 正在看
- `competing`: 正在参赛
- `custom`: 自定义状态

---

## 5. 最佳实践

### 5.1 目标标识规范

| 场景 | 格式 | 示例 |
|------|------|------|
| 频道消息 | `channel:<id>` | `channel:123456789` |
| 私信用户 | `user:<id>` | `user:987654321` |

### 5.2 用户提及

```json
{
  "action": "send",
  "channel": "discord",
  "to": "channel:123456789",
  "message": "你好 <@987654321>！"
}
```

### 5.3 写作风格

- ✅ 短小精悍，对话式
- ✅ 低仪式感
- ❌ 避免 Markdown 表格（Discord 渲染不佳）
- ✅ 使用用户提及代替 @用户名

### 5.4 多账户支持

```json
{
  "action": "send",
  "channel": "discord",
  "accountId": "secondary-bot",
  "to": "channel:123456789",
  "message": "来自备用机器人的消息"
}
```

---

## 6. 与其他 Skills 对比

| 特性 | Discord | Slack | 飞书 |
|------|---------|-------|------|
| 消息发送 | ✅ | ✅ | ✅ |
| 富媒体 | ✅ | ✅ | ✅ |
| 交互组件 | ✅ v2 | ✅ Block Kit | ✅ 卡片 |
| 投票 | ✅ 原生 | ⚠️ 需第三方 | ✅ |
| 线程 | ✅ | ✅ | ✅ |
| 搜索 | ✅ | ✅ | ✅ |
| 状态设置 | ✅ | ✅ | ⚠️ |

---

## 7. 使用场景

### 7.1 社区管理机器人

```
用户提问 → AI 分析 → Discord 回复
```

### 7.2 自动通知系统

```
CI/CD 完成 → 发送构建状态到 Discord 频道
```

### 7.3 游戏社区助手

```
/game-stats → 查询玩家数据 → Discord 嵌入式回复
```

### 7.4 多频道广播

```json
{
  "action": "broadcast",
  "channel": "discord",
  "targets": ["channel:111", "channel:222", "channel:333"],
  "message": "紧急通知：服务器维护"
}
```

---

## 8. 限制与注意事项

### 8.1 API 限制

| 操作 | 限制 |
|------|------|
| 消息长度 | 2000 字符 |
| 文件大小 | 25MB（普通）/ 500MB（Nitro） |
| 反应数量 | 每条消息 20 个 |
| Embed 字段 | 25 个 |

### 8.2 权限要求

确保 Discord Bot 拥有以下权限：
- 发送消息
- 嵌入链接
- 附加文件
- 添加反应
- 管理消息（编辑/删除他人消息）
- 创建线程

### 8.3 组件 v2 vs Embeds

- **推荐**: 使用组件 v2（`components`）
- **遗留**: Embeds 仍然支持但不推荐
- **冲突**: 不能同时使用 `components` 和 `embeds`

---

## 9. 错误处理

### 常见错误

| 错误 | 原因 | 解决方案 |
|------|------|----------|
| Missing Access | Bot 缺少权限 | 检查 Bot 角色权限 |
| Unknown Channel | 频道 ID 无效 | 验证频道 ID |
| Cannot send to user | 用户未接受私信 | 让用户先发送消息给 Bot |
| Rate Limited | 请求过于频繁 | 实现退避重试 |

---

## 10. 总结

### 优势

✅ **零配置安装**: OpenClaw 内置，只需 Token  
✅ **功能完整**: 覆盖 Discord 大部分 API  
✅ **组件 v2**: 支持最新交互式 UI  
✅ **多账户**: 支持运行多个 Bot 实例  

### 劣势

⚠️ **权限门控**: 部分功能默认关闭  
⚠️ **无独立工具**: 必须通过 `message` 工具  
⚠️ **Embeds 遗留**: 旧式嵌入支持有限  

### 推荐指数

⭐⭐⭐⭐⭐ (5/5)

**适用场景**: Discord 社区管理、游戏服务器助手、团队通知系统

---

## 11. 快速开始

### 步骤 1: 创建 Discord Bot

1. 访问 [Discord Developer Portal](https://discord.com/developers/applications)
2. 创建新应用
3. 添加 Bot 用户
4. 复制 Token

### 步骤 2: 配置 OpenClaw

```yaml
# ~/.openclaw/config.yaml
channels:
  discord:
    token: "YOUR_BOT_TOKEN"
```

### 步骤 3: 邀请 Bot 到服务器

```
https://discord.com/oauth2/authorize?client_id=YOUR_CLIENT_ID&scope=bot&permissions=2048
```

### 步骤 4: 测试发送

```json
{
  "action": "send",
  "channel": "discord",
  "to": "channel:YOUR_CHANNEL_ID",
  "message": "OpenClaw Discord Bot 已上线！🎮"
}
```

---

*分析完成于 2026-03-02 01:05 by Glint 🔺*
